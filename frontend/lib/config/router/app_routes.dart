/// Defines the route paths used in the application.
class AppRoutes {
  /// The route path for the authentication screen (login/register).
  static const String auth = '/';

  /// The route path for the auction screen.
  static const String auction = '/auction';
}
